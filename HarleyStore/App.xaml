<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:HarleyStore"
             x:Class="HarleyStore.App">
    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="Primary">#FF7A00</Color>
            <Color x:Key="Bg">#111111</Color>
            <Color x:Key="Card">#242424</Color>
            <Color x:Key="Input">#2E2E2E</Color>
            <Color x:Key="Text">#FFFFFF</Color>
            <Color x:Key="Muted">#BDBDBD</Color>

            <Style TargetType="ContentPage">
                <Setter Property="BackgroundColor" Value="{StaticResource Bg}" />
            </Style>

            <Style x:Key="PrimaryButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                <Setter Property="TextColor" Value="Black" />
                <Setter Property="CornerRadius" Value="24" />
                <Setter Property="HeightRequest" Value="52" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>

            <Style x:Key="EntryStyle" TargetType="Entry">
                <Setter Property="BackgroundColor" Value="{StaticResource Input}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="PlaceholderColor" Value="#999999" />
                <Setter Property="HeightRequest" Value="52" />
            </Style>

            <Style x:Key="CardFrameStyle" TargetType="Frame">
                <Setter Property="BackgroundColor" Value="{StaticResource Card}" />
                <Setter Property="CornerRadius" Value="22" />
                <Setter Property="Padding" Value="14" />
                <Setter Property="HasShadow" Value="False" />
                <Setter Property="BorderColor" Value="#303030" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
